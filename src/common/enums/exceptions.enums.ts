export enum HttpMessages {
    IP_NOT_FOUND = 'The provided IP address does not exist',
    COUNTRY_NOT_FOUND = 'The country code was not found',
    EXCHANGE_DATA_NOT_FOUND = 'There was an error requesting exchange data',
}