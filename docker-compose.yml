version: "3"

services:
  meli-traceip-app:
    image: nestjs-api-meli-traceip
    build:
        context: .
        dockerfile: ./Dockerfile
    ports: 
        - 5000:5000    
    volumes:
        - ./:/app/
    restart: always          
